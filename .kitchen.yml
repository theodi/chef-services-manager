---
driver:
  name: vagrant

provisioner:
  name: chef_zero

platforms:
  - name: ubuntu-14.04

suites:
  - name: production
    run_list:
      - recipe[chef_services_manager::default]
    attributes:
